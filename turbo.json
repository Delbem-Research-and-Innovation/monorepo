{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "SSH_*",
    "DATABASE_*",
    "REMOTE_*",
    "JWT_SECRET",
    "PORT",
    "GITLAB_REPO_URL",
    "GITLAB_ACCESS_TOKEN",
    "REGISTRY_*",
    "HOSTNAME",
    "AUTH_TOKEN",
    "USE_MOCK"
  ],
  "tasks": {
    "lint": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "build": {
      "outputs": ["build/**"]
    }
  }
}
